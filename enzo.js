const Variable = class { value = null; exp = new Map };


const { exp } = 

// Safe getter
parsed.set(term.var, 
parsed.get(term.var) || new Variable).get(term.var);

exp.set(term.exp,
exp.get(term.exp) || []);


const scope = { x: 1 };

// Assigning
for (const [ variable, value ] of scope) 
    parsed.get(variable).value = value;




